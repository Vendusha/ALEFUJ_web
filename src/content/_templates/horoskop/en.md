---
# Same twelve values as the Czech file, unchanged — this key (not the
# folder name) is what the site uses to match this text to the Czech
# version of the same sign, so it must be identical to the sibling cs.md.
sign: beran
lang: en
# Optional: short excerpt for the preview tile (~20–25 words). Leave it
# out and the site generates one from the first sentence of the body below.
# excerpt: "Short excerpt for the preview tile."
# Optional: set to "true" if the English text for this sign isn't ready
# yet, even if the Czech one is. The page then shows a polite "still being
# prepared" message (only when this sign happens to be "in season")
# instead of the unfinished text.
# draft: true
---

Kosťa's horoscope, unlike his verses (see the README's "Bilingual"
section), IS translated — write a real English rendering of the Czech
sibling file, not a stub. Keep the ornate, deadpan, faux-encyclopedic
register: formal, dry, ironic, never casual. It doesn't need to be a
literal translation — a joke or pun specific to Czech may need an
equivalent English one instead — but it needs to read as something Kosťa
would actually have written, not as a translation of it.

*And here (its own paragraph, entirely in italics via asterisks) the
optional ironic aside — a different, more matter-of-fact register than
the horoscope itself.*
